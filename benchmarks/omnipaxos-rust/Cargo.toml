[package]
name = "paxos-omnipaxos-benchmark"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
omnipaxos = { version = "=0.2.2", features = ["macros"] }
omnipaxos_storage = "=0.2.2"

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = 3
panic = "abort"
